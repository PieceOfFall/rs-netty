[package]
name = "tokio-bench"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
bytes = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "io-util", "sync", "time"] }
